jQuery(window).load(function() { 
"use strict";
			jQuery("#status").fadeOut(550); // will first fade out the loading animation
			jQuery("#preloader").delay(550).fadeOut(400); // will fade out the white DIV that covers the website.
		});

	